<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="colegios.list">
      <field name="name">colegios list</field>
      <field name="model">colegios.colegios</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="colegios.action_window">
      <field name="name">colegios window</field>
      <field name="res_model">colegios.colegios</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="colegios.action_server">
      <field name="name">colegios server</field>
      <field name="model_id" ref="model_colegios_colegios"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="colegios" id="colegios.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="colegios.menu_1" parent="colegios.menu_root"/>
    <menuitem name="Menu 2" id="colegios.menu_2" parent="colegios.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="colegios.menu_1_list" parent="colegios.menu_1"
              action="colegios.action_window"/>
    <menuitem name="Server to list" id="colegios" parent="colegios.menu_2"
              action="colegios.action_server"/>
    -->
  </data>
</odoo>